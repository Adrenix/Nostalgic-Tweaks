package mod.adrenix.nostalgic.client.config.gui.overlay;

/**
 * These flags can change how different overlays behave. They are used during overlay registration.
 * Boolean states are set within the default {@link Overlay} constructor.
 */

public enum OverlayFlag
{
    LOCKED
}
