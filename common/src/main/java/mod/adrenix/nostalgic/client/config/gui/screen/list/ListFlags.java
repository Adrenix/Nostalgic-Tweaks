package mod.adrenix.nostalgic.client.config.gui.screen.list;

/**
 * These flags can change how different list screens behave. They are used during list screen construction.
 * Boolean states are set within the default {@link AbstractListScreen} constructor.
 */

public enum ListFlags
{
    WATCH_PERMISSIONS
}
