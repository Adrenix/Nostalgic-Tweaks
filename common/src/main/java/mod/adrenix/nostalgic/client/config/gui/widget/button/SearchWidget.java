package mod.adrenix.nostalgic.client.config.gui.widget.button;

/**
 * The enumeration values below are state button types that will be tracked in instances of {@link StateButton}.
 *
 * Using an enumeration allows for easy checking of what a state button instance is for in the search group tab.
 * This allows for easy switching between rendering and logic.
 */

public enum SearchWidget
{
    FUZZY,
    BUBBLE,
    TAG,
    CLEAR
}
