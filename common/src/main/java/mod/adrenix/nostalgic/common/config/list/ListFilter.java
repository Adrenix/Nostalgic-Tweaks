package mod.adrenix.nostalgic.common.config.list;

/**
 * This filter enumeration instructs list screens to not include specific types of items.
 * These restrictions are useful if certain items are not to be included in some lists.
 */

public enum ListFilter
{
    NONE,
    ITEMS,
    TOOLS,
    BLOCKS
}
