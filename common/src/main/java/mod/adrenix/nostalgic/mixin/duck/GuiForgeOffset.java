package mod.adrenix.nostalgic.mixin.duck;

/**
 * Tracks height offsets of certain parts of the GUI so that it can be tracked for Forge overlay overrides.
 */

public interface GuiForgeOffset
{
    int NT$getLeft();
}
