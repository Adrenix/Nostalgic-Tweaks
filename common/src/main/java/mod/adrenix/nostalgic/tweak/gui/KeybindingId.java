package mod.adrenix.nostalgic.tweak.gui;

/**
 * The config GUI uses a keybinding identifier to determine which client keybinding a tweak is associated with.
 */
public enum KeybindingId
{
    CONFIG,
    FOG,
    NEXT_SONG,
    STOP_SONG,
    TOGGLE,
}
